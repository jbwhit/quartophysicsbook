[project]
name = "physicsbook"
version = "0.1.0"
description = "Think Like A Physicist — symbolic verification tests"
requires-python = ">=3.10"
dependencies = []

[dependency-groups]
dev = [
    "pytest>=9.0.2",
    "sympy>=1.14.0",
]
